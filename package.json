{
  "name": "midi-connector",
  "version": "1.0.0",
  "scripts": {
    "start": "yarn client:build && node --require dotenv/config server.js",
    "client:build": "cd client/; yarn build",
    "watch": "nodemon server.js",
    "test": "jest"
  },
  "dependencies": {
    "@koa/cors": "2",
    "dotenv": "8.1.0",
    "es6-promisify": "^5.0.0",
    "koa": "^2.4.1",
    "koa-body": "^2.5.0",
    "koa-mount": "^4.0.0",
    "koa-router": "^7.3.0",
    "koa-static": "^5.0.0",
    "node-cmd": "^3.0.0",
    "nodemon": "^1.11.0"
  },
  "devDependencies": {
    "jest": "^22.0.4"
  }
}
